import { CalFilterPipe } from './cal-filter.pipe';

describe('CalFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CalFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
